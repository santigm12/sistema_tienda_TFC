<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppMovil" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimaryDark">@color/toolbar</item> <!-- Define el color de la barra de estado -->
        <item name="android:statusBarColor">@color/toolbar</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.AppMovil" parent="Base.Theme.AppMovil"/>

</resources>